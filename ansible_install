ansible

# yum install -y epel-release
# yum install ansible
# yum install -y python2 python-simplejson 
 or # ansible myhost --sudo -m raw -a "yum install -y python2 python-simplejson" 

# ssh-keygen
# ssh-copy-id -i /root/.ssh/id_rsa your_ip
# chmod 600 /root/.ssh/authorized_keys                # for every linux
# vim /etc/ansible/hosts
  add your_ip and can create group    
  
# ssh-agent bash
# ssh-add ~/.ssh/id_rsa

# ansible all -m ping 
# ansible 192.168.64.160 -m command -a 'w'  

文档使用：
ansible-doc -l   列出所有的模块
ansible-doc cron  查看指定模块的文档  
